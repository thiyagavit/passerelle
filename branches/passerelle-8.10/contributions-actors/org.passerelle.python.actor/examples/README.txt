This folder contains a trivial example Python script for the Python actor, and an example model file.

The actor must be configured with the path to the script file, so you should replace the dummy path 
by something that corresponds to your development/installation structure.